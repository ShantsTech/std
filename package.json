{
  "name": "std",
  "license": "GPL-3.0",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "components",
    "storybook"
  ],
  "scripts": {
    "dev": "yarn workspace std-storybook start",
    "test:unit": "yarn workspace std-components test:unit",
    "test:cov": "yarn workspace std-components test:unit --coverageDirectory coverage --coverage",
    "lint": "yarn workspace std-components lint",
    "tsc": "yarn workspace std-components tsc"
  },
  "engines": {
    "node": ">=10.19.0",
    "yarn": ">=1.22.4"
  },
  "dependencies": {
    "babel-plugin-module-resolver": "^4.1.0"
  }
}
